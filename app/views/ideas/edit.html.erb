<!DOCTYPE html>
<h1>Edit Idea</h1>

<%= simple_form_for @idea, method: :patch do |f| %>
<%= f.input :description %>
  <%= f.input :author %>
  <%= f.submit 'Update', class: 'btn btn-primary' %>
<% end %>